1. Тест 2 (получение объявления по идентификатору) выдаёт ошибку "поле likes обязательно", что означает likes не может быть равно 0, но такой сценарий должен быть. Аналогичная ошибка в полях Contacts и ViewCount <br>
2. Тест 2. Сервер возвращает объект, а не массив в GET/item/{id} <br>
3. Тест 3. sellerId=0, а не тот Id который отправлен <br>
4. Тест 3. Сервер принимает SellerID, но сохраняет как 0 или другое значение и не возвращает созданное побъявление по запросу GET <br>
5. Тест 4. /api/1/statistic/{id} требует, чтобы id был в формате UUID, но в ответе на POST приходит строка вида "abc-123"
